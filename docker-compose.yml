services:
  web:
    build: .
    environment:
      - PORT=8080
    ports:
      - "8764:8080"
    healthcheck:
      test: ["CMD", "python", "probe.py"]
      interval: 5s
      timeout: 3s
      retries: 5
